#!/usr/bin/env python
# -*- coding:utf-8 -*-
import sys,os
#import math,re,datetime
from ngramCrewler import *


def setCountDic(n):
    countdic = dict()
    sumdic = dict()

    f = open("../data/"+str(i)+"gram.tsv","r")
    for line in f:
        items = line.strip().split("\t")
        ngramtuple = tuple(items[:-1])
        counts = items[-1]

        if n > 1:
            if tuple(items[:-2]) in sumdic:
                

if __name__ == "__main__":
